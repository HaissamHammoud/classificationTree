#include<stdio.h>
#include<stdlib.h>
#include"spine.h"

int main()
{
    //Loading
    int n, i;
    Table * l = newData();
    char opt= 'a';
    BackDataSet* nova ;
    BackDataSet* movel ;
    LoadSpineDataCsv(l);

    // create the split data
    for(i = 0 ; i < 12 ; i++)
    {
      
    }
}